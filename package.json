{
  "name": "antigravity-quiz-app",
  "version": "1.0.0",
  "description": "Interactive quiz application with WebSocket support for schools",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build:tv": "cd client-tv && npm install --include=dev && npm run build",
    "build:mobile": "cd client-mobile && npm install --include=dev && npm run build",
    "build:all": "npm run build:tv && npm run build:mobile",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "keywords": [
    "quiz",
    "websocket",
    "education",
    "real-time"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.13.1",
    "socket.io": "^4.8.1",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
